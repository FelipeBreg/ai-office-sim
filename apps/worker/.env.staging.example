# =============================================================================
# AI Office Sim — Staging Environment (Railway Worker)
# =============================================================================
# Set these as environment variables in the Railway staging service.
# All values below are placeholders — replace with real staging credentials.
# =============================================================================

# ── Database (Supabase PostgreSQL — same as web) ──
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ── Redis / BullMQ (Upstash) ──
REDIS_URL=rediss://default:[password]@[endpoint].upstash.io:6379
BULLMQ_REDIS_URL=rediss://default:[password]@[endpoint].upstash.io:6379

# ── Worker Configuration ──
WORKER_CONCURRENCY=5
PORT=4000

# ── Clerk Authentication ──
CLERK_SECRET_KEY=sk_test_xxxxx

# ── WhatsApp (Z-API — staging instance) ──
WHATSAPP_ZAPI_BASE_URL=https://api.z-api.io/instances/STAGING_INSTANCE_ID/token/STAGING_TOKEN
WHATSAPP_ZAPI_TOKEN=staging-zapi-token-xxxxx

# ── LLM / AI ──
OPENAI_API_KEY=sk-xxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxx

# ── Observability ──
HELICONE_API_KEY=sk-helicone-xxxxx
SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ── Environment ──
NODE_ENV=production
